[package]
name = "release-doc-copy-paste-canary"
version = "0.0.0"
publish = false
edition = "2024"

[workspace]

[dependencies]
uselesskey = { version = "{{version}}", default-features = false, features = ["token"] }
