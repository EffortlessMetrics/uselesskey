[package]
name = "adapter-rustls-canary"
version = "0.0.0"
publish = false
edition = "2024"

[workspace]

[dependencies]
uselesskey-core = { version = "{{version}}" }
uselesskey-x509 = { version = "{{version}}" }
uselesskey-rustls = { version = "{{version}}", features = ["tls-config", "rustls-ring"] }
