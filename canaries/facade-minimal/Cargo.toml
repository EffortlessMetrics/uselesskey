[package]
name = "canary-facade-minimal"
version = "0.0.0"
publish = false
edition = "2024"

[dependencies]
uselesskey = { path = "../../crates/uselesskey", features = ["rsa", "token", "x509"] }

[workspace]
