[package]
name = "facade-minimal-canary"
version = "0.0.0"
publish = false
edition = "2024"

[workspace]

[dependencies]
uselesskey = { path = "../../crates/uselesskey", features = ["rsa", "token", "x509"] }
