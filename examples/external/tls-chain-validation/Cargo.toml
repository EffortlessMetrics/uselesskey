[package]
name = "uselesskey-external-tls-chain-validation"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
uselesskey = { version = "0.10.0", default-features = false, features = ["x509"] }
uselesskey-rustls = { version = "0.10.0", features = ["tls-config", "rustls-ring"] }

[workspace]
