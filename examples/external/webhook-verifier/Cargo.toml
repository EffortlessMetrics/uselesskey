[package]
name = "uselesskey-external-webhook-verifier"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
uselesskey = { version = "0.10.0", default-features = false, features = ["webhook"] }

[workspace]
