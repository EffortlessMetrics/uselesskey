# uselesskey-ssh

Deterministic and random OpenSSH fixtures for tests.

- `authorized_keys` public key lines
- OpenSSH private key export
- OpenSSH certificates (principals, validity windows, critical options, extensions)

This crate is test-fixture infrastructure and is **not** for production secrets.
