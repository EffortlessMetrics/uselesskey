# uselesskey-core-pgp-spec

Core OpenPGP fixture spec model shared by `uselesskey` crates.

This crate provides a small, stable `PgpSpec` type so generation crates and adapters can share deterministic spec encodings without duplicating logic.
