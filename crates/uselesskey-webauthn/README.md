# uselesskey-webauthn

Deterministic WebAuthn registration/assertion fixtures for tests.

This crate provides fixture data that is realistic in shape but intentionally
is not a full authenticator or FIDO2 server implementation.
