# uselesskey-x509-negative

X.509-focused negative fixture helpers for `uselesskey`.

This microcrate provides certificate-scoped adapters over shared core negative
helpers and re-exports `X509Negative` policy variants for convenient X.509 test
fixture construction.
