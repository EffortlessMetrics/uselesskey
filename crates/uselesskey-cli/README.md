# uselesskey-cli

Export and manifest helpers for handing `uselesskey` fixtures to local files,
Kubernetes YAML, Vault KV payloads, and SOPS-ready skeletons.

This crate is intentionally **not** a secret manager: it generates output once
and stops.
