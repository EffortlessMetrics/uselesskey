# uselesskey-cli

CLI-oriented export surface for handing generated fixture data to existing secret delivery toolchains.

This crate focuses on **write + reference + manifest** behavior and intentionally avoids provider custody APIs.
