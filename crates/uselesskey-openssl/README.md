# uselesskey-openssl

OpenSSL adapter crate for `uselesskey` fixtures.

This crate converts fixture outputs into native OpenSSL types (`PKey`, `X509`) and includes basic sign/verify helpers for tests.
