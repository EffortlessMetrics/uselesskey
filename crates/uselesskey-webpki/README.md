# uselesskey-webpki

`webpki` adapter helpers for `uselesskey` X.509 fixtures.

This crate builds trust anchors and end-entity verification checks from existing fixture certificates, including support for negative-chain assertions.
