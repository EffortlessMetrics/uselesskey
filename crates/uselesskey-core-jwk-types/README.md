# uselesskey-core-jwk-types

Core typed JWK and JWKS models used by `uselesskey` fixture crates.

This crate contains shape-only types (`RsaPublicJwk`, `PrivateJwk`, `Jwks`, etc.) and
their JSON conversion/display helpers.
