# uselesskey-jwk

Typed JWK/JWKS helpers for `uselesskey`.

This crate provides small structs and builders for JSON Web Keys so fixture
crates can return typed values while still offering `serde_json::Value` helpers.

Not for production key handling.
