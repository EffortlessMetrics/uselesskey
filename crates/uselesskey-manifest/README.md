# uselesskey-manifest

Machine-readable fixture receipts and bundle manifests for `uselesskey`.

This crate defines stable schema types (`FixtureReceipt`, `OutputFile`, `BundleReceipt`) and a canonical JSON writer so manifests are reproducible across environments.
