# uselesskey-manifest

Stable, machine-readable fixture manifest types for `uselesskey`.

This crate defines `FixtureReceipt` and `BundleReceipt` plus canonical JSON writers.
It focuses on schema stability and deterministic ordering, independent from CLI UX.
