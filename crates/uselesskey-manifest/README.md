# uselesskey-manifest

Stable, machine-readable fixture receipts for `uselesskey` outputs.

This crate defines receipt schema types and canonical JSON writing helpers so
fixture bundles can be inspected and reproduced across tools and languages.
